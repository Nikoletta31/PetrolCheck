// STUB — implementation owned by Anouar (src/composables/useFilters.js)
// Replace this file with the real filters implementation.
import { reactive } from 'vue'

export function useFilters() {
  const filters = reactive({
    carburants: [],
    rayon: 10,
  })

  return { filters }
}
